"""Default values for litefupzl read-only oneshot mode."""

SESSION_DURATION_MINUTES = 40
FINGERPRINT_VIEWPORT = [1920, 1080]
FINGERPRINT_TIMEZONE = "America/Los_Angeles"
FINGERPRINT_LOCALE = "en-US"
